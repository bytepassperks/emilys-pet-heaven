const SANITY_CONFIG={projectId:'kxhc14m8',dataset:'production',apiVersion:'2024-01-01',useCdn:true};function buildSanityUrl(query,params={}){const baseUrl=`https:const encodedQuery=encodeURIComponent(query);let url=`${baseUrl}?query=${encodedQuery}`;Object.keys(params).forEach(key=>{url+=`&$${key}="${encodeURIComponent(params[key])}"`;});return url;}
function buildImageUrl(imageRef,options={}){if(!imageRef||!imageRef.asset||!imageRef.asset._ref){return'/images/placeholder-blog.jpg';}
const ref=imageRef.asset._ref;const[,id,dimensions,format]=ref.split('-');let url=`https:const transforms=[];if(options.width)transforms.push(`w=${options.width}`);if(options.height)transforms.push(`h=${options.height}`);if(options.quality)transforms.push(`q=${options.quality}`);if(options.fit)transforms.push(`fit=${options.fit}`);if(transforms.length>0){url+='?'+transforms.join('&');}
return url;}
async function fetchBlogPosts(limit=10,offset=0){const query=`*[_type=="blogPost"&&status=="published"]|order(publishedAt desc)[${offset}...${offset+limit}]{_id,title,slug,excerpt,featuredImage,publishedAt,"author":author->name,"authorImage":author->image,"category":category->title,"categorySlug":category->slug.current,language}`;try{const response=await fetch(buildSanityUrl(query));const data=await response.json();return data.result||[];}catch(error){console.error('Error fetching blog posts:',error);return[];}}
async function fetchBlogPostBySlug(slug){const query=`*[_type=="blogPost"&&slug.current==$slug][0]{_id,title,slug,excerpt,body,featuredImage,metaTitle,metaDescription,focusKeyword,keywords,publishedAt,"author":author->{name,bio,image,role},"category":category->{title,slug,description},language,socialMedia}`;try{const url=buildSanityUrl(query,{slug});const response=await fetch(url);const data=await response.json();return data.result;}catch(error){console.error('Error fetching blog post:',error);return null;}}
async function fetchBlogPostsByCategory(categorySlug,limit=10){const query=`*[_type=="blogPost"&&status=="published"&&category->slug.current==$categorySlug]|order(publishedAt desc)[0...${limit}]{_id,title,slug,excerpt,featuredImage,publishedAt,"author":author->name,"category":category->title}`;try{const url=buildSanityUrl(query,{categorySlug});const response=await fetch(url);const data=await response.json();return data.result||[];}catch(error){console.error('Error fetching blog posts by category:',error);return[];}}
async function fetchCategories(){const query=`*[_type=="category"]|order(title asc){_id,title,slug,description,icon,"postCount":count(*[_type=="blogPost"&&references(^._id)&&status=="published"])}`;try{const response=await fetch(buildSanityUrl(query));const data=await response.json();return data.result||[];}catch(error){console.error('Error fetching categories:',error);return[];}}
async function fetchRecentPosts(limit=5){const query=`*[_type=="blogPost"&&status=="published"]|order(publishedAt desc)[0...${limit}]{_id,title,slug,featuredImage,publishedAt}`;try{const response=await fetch(buildSanityUrl(query));const data=await response.json();return data.result||[];}catch(error){console.error('Error fetching recent posts:',error);return[];}}
function portableTextToHtml(blocks){if(!blocks||!Array.isArray(blocks))return'';return blocks.map(block=>{if(block._type==='block'){const style=block.style||'normal';let text='';if(block.children){text=block.children.map(child=>{let content=child.text||'';if(child.marks&&child.marks.length>0){child.marks.forEach(mark=>{if(mark==='strong')content=`<strong>${content}</strong>`;if(mark==='em')content=`<em>${content}</em>`;if(mark==='underline')content=`<u>${content}</u>`;});}
return content;}).join('');}
switch(style){case'h2':return`<h2>${text}</h2>`;case'h3':return`<h3>${text}</h3>`;case'h4':return`<h4>${text}</h4>`;case'blockquote':return`<blockquote>${text}</blockquote>`;default:return`<p>${text}</p>`;}}
if(block._type==='image'){const imageUrl=buildImageUrl(block,{width:800,quality:80});const alt=block.alt||'Blog image';const caption=block.caption?`<figcaption>${block.caption}</figcaption>`:'';return`<figure><img src="${imageUrl}"alt="${alt}"loading="lazy">${caption}</figure>`;}
return'';}).join('\n');}
function formatDate(dateString){if(!dateString)return'';const date=new Date(dateString);return date.toLocaleDateString('en-IN',{year:'numeric',month:'long',day:'numeric'});}
async function getTotalPostCount(){const query=`count(*[_type=="blogPost"&&status=="published"])`;try{const response=await fetch(buildSanityUrl(query));const data=await response.json();return data.result||0;}catch(error){console.error('Error getting post count:',error);return 0;}}
window.SanityClient={fetchBlogPosts,fetchBlogPostBySlug,fetchBlogPostsByCategory,fetchCategories,fetchRecentPosts,buildImageUrl,portableTextToHtml,formatDate,getTotalPostCount};